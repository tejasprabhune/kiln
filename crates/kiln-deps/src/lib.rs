//! Dependency resolution for `kiln`. Wraps `bender`.
//!
//! This crate is a stub at M0; populated from M4 onward.

pub fn placeholder() -> &'static str {
    "kiln-deps: not yet implemented"
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder_is_stable() {
        assert_eq!(super::placeholder(), "kiln-deps: not yet implemented");
    }
}
